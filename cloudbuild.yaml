steps:
  - name: 'gcr.io/cloud-builders/npm'
    args: ['install']
  - name: 'gcr.io/cloud-builders/npm'
    args: ['run', 'build']
  - name: 'node'
    entrypoint: 'npx'
    args: ['firebase', 'deploy', '--project=mmm-nevo', '--token=${_FIREBASE_TOKEN}']

options:
  logging: CLOUD_LOGGING_ONLY
